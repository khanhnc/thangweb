import { ServiceChgColorDirective } from './service-chg-color.directive';

describe('ServiceChgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceChgColorDirective();
    expect(directive).toBeTruthy();
  });
});
